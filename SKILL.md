---
name: insight-voice
description: 分析单份或多份中文1v1、半结构式访谈文稿，为产品研究、学术与通用定性研究生成有证据、可追溯、可复核的单场观察、跨访谈主题或既有结论核查。适用于用户、客户、服务体验、毕业论文、课题、事件经历及田野文字访谈；不用于音视频转写、定量统计、市场规模推断、医学诊断、法律裁决、产品路线图或自动需求优先级。
---

# 洞察之声

## 目标

将中文1v1或半结构式访谈转化为有证据、可追溯、可复核的研究观察与跨访谈主题。保留上下文，区分事实、感知、推断与假设，不替代产品、学术或专业领域的最终判断。

## 必须读取

开始正式分析前：

1. 读取 [references/evidence-rules.md](references/evidence-rules.md)，执行证据、计数、隐私与拒答规则。
2. 读取 [references/output-schema.md](references/output-schema.md)，根据访谈数量选择单场或批量输出协议。
3. 读取 [references/analysis-modes.md](references/analysis-modes.md)，选择产品研究、通用定性研究或已有结论核查模式。

不要将测试集、人工标准答案或评估结论作为分析依据。

## 工作流程

### 1. 确认任务

- 判断用户要进行产品研究、通用定性研究或已有结论核查；用户未指定时，产品、功能或使用体验相关材料默认产品研究，论文、课题、社会现象或人物经历默认通用定性研究，要求核对既有结论时使用结论核查。
- 仅处理中文文字版访谈；允许少量英文术语。
- 对混合任务先完成访谈洞察，再明确区分PRD、路线图或市场判断等下游任务。
- 没有访谈文稿时，请求用户上传或粘贴文稿，不自行生成用户观点。
- 只有音频、视频或不可提取文字的扫描件时，说明需要先获得文字转写稿。

### 2. 执行输入预检

将输入标记为以下一种状态：

- **A级｜可分析**：文字可读，访谈边界清晰，访谈者与受访者可区分，存在有效受访者回答。
- **B级｜带限制分析**：缺少部分元数据、文稿不完整、少量说话人或转写内容不明确，或未经录音核对的自动转写存在明显识别、断句、说话人错位或关键信息不清，但核心证据仍可确认。自动转写本身不必然降级。
- **C级｜暂停分析**：没有文稿、文件不可读、几乎没有受访者回答、多名受访者无法区分，或说话人身份普遍无法确认。

对A级继续分析；对B级先列出限制再分析；对C级停止洞察提取，只说明阻塞原因和补充要求。
多场提问方式明显不一致且影响横向比较时，保留分析但在资料限制中说明；少量无法确认段落不得作为核心证据。

### 3. 标准化资料

- 保留每份文稿的原始边界，不把多场访谈混成一个受访者。
- 使用用户提供的访谈ID；缺失时按输入顺序生成 `INT-001`、`INT-002`。
- 对文件型材料，必须先运行 `scripts/build_evidence_ledger.py` 建立冻结证据账本；不得绕过账本直接生成完整报告。账本或说话人解析失败时按B级或C级处理，并列出受影响文件与段落。
- 保留原文已有的稳定Turn ID；缺失时从“访谈正文”的第一条实际对话开始，为**全部口头对话轮次**按原文顺序生成 `INT-001-T001` 格式的定位ID。
- 访谈者、受访者、身份未标记发言及其他对话参与者都占用连续Turn编号；不能只按受访者回答编号。访谈者Turn只作上下文，不因此获得用户证据资格。
- 标题、元数据和纯转写状态说明不占用Turn编号。需要定位研究员笔记或系统说明时，分别使用 `INT-001-NOTE001`、`INT-001-SYS001`，不得导致后续对话Turn错位。
- 编号不因内容是否被引用、证据是否有效或报告排序而变化。建立编号后先保存“定位ID—说话人—原文”映射，再进行洞察提取。
- 账本中的Turn ID必须为完整三位格式（如`INT-001-T035`），并在本次任务中冻结；报告引用、结构化结果和校验均以账本为准。
- 每次出现证据ID都必须写完整ID，例如`INT-001-T004`、`INT-001-T022`；不得在同一串中将后续ID缩写为`T022`、`/T022`或任何省略形式。
- 区分访谈者、受访者、研究员笔记、系统说明和身份不明内容。
- 身份未标记内容默认不作为正式用户证据；仅在连续对话足以可靠判断时标记为“自动识别，推测为受访者”，并仅作低置信度的弱上下文线索。它不得单独支撑核心洞察、痛点或产品结论。
- 未标记内容不得单独生成`INS-`洞察，也不得作为假设的唯一依据；未获明确受访者证据独立印证时，只能进入资料限制、信息缺口或待确认事项。
- 仅在输出中遮挡敏感个人信息，不擅自改写原始文件。
- 只使用用户提供的受访者标签，不根据语言风格推测年龄、职业、人格或用户分群。

### 4. 逐场提取

先独立分析每场访谈，再进行任何跨访谈汇总。

对每场访谈：

1. 按所选模式提取产品痛点/使用经历，或主题、现象、经历、观点与意义建构。
2. 提取发生情境、触发因素、当时行为或应对、后续影响、支持资源、阻碍与条件边界。
3. 产品模式才使用当前解决方式、替代工具或规避行为；通用模式不强制使用产品语言。
4. 产品模式分开记录用户方案与底层问题；通用模式分开记录受访者解释与研究者推断。
5. 保存支持结论的原话、证据ID和必要上下文。
6. 记录受访者的修正、犹豫、条件限制、前后矛盾和无法确认的信息。
7. 将结论标记为明确事实、合理推断、待验证假设或证据不足。

只将已经发生或正在实施的行为写为“当前解决方式”。未来计划、按需使用意向或假设性做法必须保留为意向或假设；实际使用频率未被直接说明时写“未确认”。

### 5. 批量聚合

输入两场及以上访谈时：

- 只在逐场分析完成后聚合。
- 按相同的底层问题与使用场景合并主题；保留重要场景差异。
- 将候选结论拆成原子判断，并对每位独立受访者建立互斥覆盖矩阵：`S`明确支持、`P`部分或条件式支持、`C`相反或纠正、`M`同一访谈中同时存在支持与相反表达、`U`未覆盖或无法判断。
- 每位受访者每项洞察只能有一个主状态，且`S+P+C+M+U=N_total`。只有`S`直接计入样本覆盖`n/N_total`；`P`必须收窄结论或单列为条件式支持；`C`进入相反意见；`M`单列为混合观点；`U`既不作支持也不作反对。
- 使用“全部”“共同”“都”“一致”等词前，确认相关受访者不存在`P`、`C`或`U`。不同机制只在足够保守的上位主题下并列，并明确差异；不能合并成同一个Bug或方案。
- 默认使用“样本覆盖 `n/N_total`”：`n`为去重后的明确支持者数，`N_total`为本次有效独立受访者总数。只有部分人覆盖某议题时，另列议题覆盖基数及支持、反对、不确定人数。
- 同一人重复表达同一主题只计一次。为每项聚合洞察保存明确支持者、条件式支持者、相反意见者、未覆盖人数、全部关联证据ID和至少一条代表性证据。
- 聚合后回看全部单场结果，避免漏算符合主题定义的受访者；若纳入某人会使结论过宽，则拆分洞察而不是扩大措辞。
- 单独呈现相反意见、少数观点、混合观点和同一受访者的自我修正。
- 只有用户提供可验证的分组标签时，才比较不同群体。
- 不按提及人数自动排列产品优先级，不把高频等同于高价值。

### 5.1 大量访谈强制流程

按以下顺序完成：输入预检 → 生成全部证据账本 → 确认文件数、有效访谈数与独立受访者数 → 逐场生成并保存结构化结果 → 生成覆盖矩阵 → 跨访谈洞察 → 运行证据与计数校验 → 校验为零错误后渲染Markdown。长文稿按完整访谈分批，不在单场中途切断；必须列出已处理、未处理和排除文件，未完成全部材料前不得输出全量覆盖人数或总体主题排名。

### 6. 检查已有结论

用户提供研究结论并要求核查时：

- 将每条结论拆成可验证的原子判断。
- 查找直接证据、间接证据、相反证据和缺失证据。
- 标记为“支持”“部分支持”“不支持”或“资料不足”。
- 不为了匹配用户结论而选择性忽略访谈内容。

### 7. 执行终检

输出前逐项检查：

- 每项正式洞察是否有有效证据ID。
- 引用是否来自受访者并保持原意。
- Turn ID是否按完整对话轮次编号，而不是受访者回答序号；每条引用是否与对应Turn中的实际原文一致。
- 同一Turn ID是否只对应一段原文，同一段原文是否保持同一定位ID；发现错配时，是否已同步修复全部引用。
- 访谈者提问、研究员笔记和身份不明内容是否被错误当作用户证据。
- 自动识别的说话人内容是否仅作为低置信度弱上下文线索，且未被单独用作核心证据。
- 是否将用户未看到、未找到、未记得或猜测的问题错误写成产品的客观状态。
- 是否将未来使用意向错误写成实际行为、留存、流失、使用频率或用户类型。
- 人数是否按独立受访者计算。
- 每项聚合洞察是否完成互斥的`S/P/C/M/U`核验，五类状态之和是否等于有效独立受访者总数，且没有把部分支持、相反意见、混合观点或未覆盖写成共同结论。
- `n`是否等于明确支持者ID去重后的数量，`N_total`是否等于有效独立受访者总数，支持者名单中的每个人是否至少关联一条有效证据。
- 是否回看全部单场结果，避免漏算；议题未覆盖者是否没有被写成“没有需求”。
- 是否保留反例、矛盾、条件和不确定措辞。
- 是否把推断、付费意愿、因果关系或市场判断写成事实。
- 是否把用户提出的方案写成已验证的最佳方案，或输出了P0/P1、路线图、产品指标或确定性功能决策；如存在机会，仅以待验证假设和中立验证方式呈现。
- 是否遮挡敏感个人信息。
- 是否清楚说明资料限制和未提及信息。
- 所有正式洞察（包括批量）是否统一使用`INS-`，所有假设是否使用`HYP-`，且已运行`validate_analysis_evidence.py`并获得零错误。

任一项不满足时，修正、降级为假设或标记证据不足；对文件型材料，账本或自动校验未运行、未通过时不得声称“分析完成”，应说明当前无法完成的校验条件，不要用流畅措辞掩盖证据缺口。

### 8. 生成结果

- 单场访谈使用单场报告结构。
- 多场访谈使用所选模式的批量报告结构。
- 默认输出中文Markdown。
- 用户明确要求接入下游Workflow时，先输出结构化JSON，经`validate_analysis_evidence.py`校验为零错误后再渲染同字段Markdown。
- 用户要求简版时可以压缩说明，但不得删除证据ID、资料限制、反例或事实/假设标记。

## 长文稿处理

- 优先按完整访谈分批，不在语义未结束处切断证据。
- 先保存每场结构化结果，再汇总，不依赖对全文的模糊记忆。
- 无法在一次运行中处理全部资料时，明确列出已处理与未处理访谈，停止生成全量结论，并请求继续下一批。
- 未分析完全部访谈前，不输出完整覆盖人数或全体主题排名。

## 固定边界

- 不转写音视频。
- 不设计访谈提纲、招募问卷或研究计划。
- 不分析定量问卷、NPS或市场规模。
- 不自动生成PRD、原型、路线图或需求优先级。
- 可以输出用户痛点、场景、当前解决方式、用户提出的方案、相反意见、信息缺口、待验证假设、中立追问，以及可用性测试或行为数据验证方式；通用模式则输出与研究问题对应的经历、观点、情境、应对、影响、资源、阻碍、意义建构与主题。
- 可以输出用户痛点、场景、当前解决方式、用户提出的方案、相反意见、信息缺口、待验证假设、下一轮中立追问，以及可用性测试或行为数据验证方式。
- 不输出P0/P1/P2、需求优先级、开发排期、路线图、确定性功能设计方案、直接指定的产品核心指标或“应立即上线”等产品决策；将可能机会转为说明依据、缺失证据、验证方式及支持或推翻条件的`HYP-`假设。
- 不诊断人格、心理状态、疾病或“真实动机”。
- 不推断未明确表达的付费意愿。
- 不编造用户、用户原话、用户标签或样本代表性。
